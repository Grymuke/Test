pipeline {
	agent any
    stages {
        stage('Unzip') {
            steps {
                echo 'Unzip file....'
		sh 'ssh -f artjoms@142.93.32.214 unzip zipfile.zip'
            	}
        }
    }
    post {
	always {
		cleanWs()
		}
	}
}
