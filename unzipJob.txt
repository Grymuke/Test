pipeline {
	agent any
    stages {
        stage('Unzip') {
            steps {
                echo 'Unzip file....'
		sh 'ssh -f artjoms@142.93.32.214 "bash unzip.txt"'
            	}
        }
    }
    post {
	always {
		cleanWs()
		}
	}
}
