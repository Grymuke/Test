pipeline {
	agent {label 'SlaveServer'
  }
    stages {
        stage('Unzip') {
            steps {
	    	s 'whoami'
                echo 'Unzip file....'
                sh 'java -version'
                sh 'pwd'
		
		
            	}
        }
    }
    //post {
	//always {
		//cleanWs()
		//}
	//}
}
