pipeline {
	agent {label 'SlaveServer'
  }
    stages {
        stage('Unzip') {
            steps {
	    	sh 'whoami'
                echo 'Unzip file....'
                sh 'java -version'
                sh 'pwd'
		sh 'mkdir test'
		sh 'cd test'
		sh 'mkdir $(date +%Y%m%d_%H%M%S)'
		
            	}
        }
    }
    //post {
	//always {
		//cleanWs()
		//}
	//}
}
