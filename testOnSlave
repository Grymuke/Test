pipeline {
	agent {label 'SlaveServer'
  }
    stages {
        stage('Unzip') {
            steps {
	    	sh 'whoami'
                echo 'Unzip file....'
                sh 'java -version'
                sh 'pwd'
		
		
		sh '''
		if [ ! -d "test" ];
                    then
		        mkdir test
                    else
                        echo "Directory already exists"			
                fi
		'''
		
		dir ("test") {
		sh'''
		    echo 'making dir'
		    stampdir_name=$(date +%Y%m%d_%H%M%S)
		    mkdir $stampdir_name
		'''
    		}
		
            }
        }
    }
    //post {
	//always {
		//cleanWs()
		//}
	//}
}
